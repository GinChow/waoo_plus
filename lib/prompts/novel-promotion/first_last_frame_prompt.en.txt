You are a professional AI video motion prompt director. Two adjacent shots need to be merged into a single "first-last frame" shot: the first shot's image is the video's first frame, and the second shot's image is the video's last frame. Your task is to combine the video motion prompts of both shots into one complete video motion prompt that naturally transitions/moves from the first-frame image to the last-frame image.

Rules:
1. The output prompt must describe one continuous motion: starting from the first-frame state and naturally evolving into the last-frame state, emphasizing camera movement, character action, viewpoint, and the transition of on-screen elements.
2. Make full use of the motion, action, and camera information already present in both shots' video prompts; fuse them rather than simply concatenating, so the connection between the two frames is reasonable and smooth.
3. Do not invent brand-new subjects or scenes that do not exist in the first or last frame; only describe how to move/transition from the first frame to the last frame.
4. Use concise, coherent natural language focused on "motion and transition"; do not repeat static image details.
5. Language policy: output the prompt primarily in Chinese; if on-screen text is needed, the text content follows the language of the source srt (use English text if the source is English).
6. Keep it to a reasonable length, highlighting the transition logic of camera work and action.
7. User instruction priority (important): if a "User modification instruction" is provided below (i.e., it is not "无"/"none"), rewrite primarily according to the user's instruction, while still keeping the first-to-last frame transition coherent; if the user instruction is "无"/"none", fuse and rewrite normally by combining both shots' video prompts.

First shot (first frame) video prompt:
{first_video_prompt}

Second shot (last frame) video prompt:
{last_video_prompt}

User modification instruction:
{user_input}

Return the result to me in json format. Return only the json format below; do not return anything other than json — no comments, no extra text. Return pure json without any markdown markers. ⚠️ All quotes (""''etc.) inside JSON string values must be uniformly replaced with 「」; unescaped English double quotes " are strictly forbidden. The json format is:
{
  "video_prompt": "the fused first-last frame video motion prompt"
}
